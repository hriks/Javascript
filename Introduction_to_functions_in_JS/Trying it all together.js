// Nicely written function:
var calculate = function (number) {
    var val = number * 10;
    console.log(val);
};

// Badly written function with syntax errors are Corrected!

var greeting = function(name) {
    console.log(name);
};
calculate(3);
greeting("Saara");
